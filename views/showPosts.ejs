<%- include('./includes/header.ejs') %>
<%- include('./includes/navbar.ejs') %>

<section class="main-container">
    <div class="contain-fluid">
        <div class="row">
            <div class="col-lg-10">
                <% if(posts.length > 0) {%>
                    <% posts.forEach(post => %{ %>
                            <div class="card">
                                <a href="/post/<%= post._id%>" class="btn btn-primary">
                                    <div class="card-body">
                                        <h5 class="card-title"><%= post.title%></h5>
                                        <div style="overflow: hidden;white-space: nowrap;max-height: 80px;text-overflow: clip;">
                                            <p class="card-text"><%- post.description></p>
                                        </div>
                                    <% if(post.author == isAuth) {%>
                                        <p>Action</p>
                                        <form action="/post-delete" method="POST" class="login-form">
                                            <input type="hidden" valeu="<%= post._id %>" name="postId">
                                            <input type="hidden" valeu="<%= csrfToken %>" name="_csrf">
                                            <button class="submit-button" type="submit">Delete</button>
                                        </form>
                                    <% } else { %>

                                    <% } %>
                                    </div>
                                </a>
                            </div>
                <% } } %>
                <% } else { %>
                    <h1>No Posts</h1>
                <% } %>
            </div>
            <div class="col-lg-2">
                <h1>Lodjdlkfkfjm safjfkjsdsdfksdjfmsaf.sdfklsdjf,saflksdfjwoir,sfdlsakfj;sldkf;sdkfklsdf</h1>
            </div>
        </div>
    </div>
</section>